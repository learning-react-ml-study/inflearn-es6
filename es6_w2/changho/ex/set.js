// set 

let newSet = new Set();

newSet.add('df');
newSet.add('df');
console.log(newSet) // Set(1) {"df"}
console.log(newSet) // Set(1) {"df"}

// set Test;  // 실패 ㅠㅠ

const SETTING = {
    name: 'lotto',
    count: 6,
    maxNumber: 45
}
const newSet = new Set();
const {name, count, maxNumber} = SETTING;
console.log(name, count)

// newSet.size = count;

